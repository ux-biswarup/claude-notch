{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the overlay window. App commands are allowed by default; only core event/window access is needed.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
